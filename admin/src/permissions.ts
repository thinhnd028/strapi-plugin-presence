export const pluginPermissions = {
  accessVersionHistory: [{ action: 'plugin::presence.access-version-history', subject: null }],
  accessActionHistory: [{ action: 'plugin::presence.access-action-history', subject: null }],
  useRestore: [{ action: 'plugin::presence.restore', subject: null }],
};
